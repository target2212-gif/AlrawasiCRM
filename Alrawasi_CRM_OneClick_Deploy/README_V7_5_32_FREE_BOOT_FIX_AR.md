# V7.5.32 — إصلاح تشغيل Render Free

- إصلاح خطأ `no such table: users` في الإقلاع.
- لا يتم إنشاء SQLite فارغ ثم استيراد `app.py` عليه.
- في Render Free تستخدم القاعدة التشغيلية `/tmp/alrawasi-data/crm.db` وتُنسخ من `crm.db` المرفقة فقط عند عدم وجودها.
- في الخطة المدفوعة مع `/var/data` تبقى حماية Data-Lock الصارمة كما هي.
- لا يوجد Persistent Disk في الخطة المجانية؛ لذلك إعادة بناء/نشر الخدمة قد تعيد القاعدة إلى النسخة المرفقة. استخدم تصدير JSON بعد التنقيح للاحتفاظ بأحدث نسخة قابلة للاستعادة.
