# تلال الرواسي CRM — V7.5.12 GitHub Ready

هذه النسخة مخصصة للرفع إلى GitHub/Render بدون ملف الإثراء الكبير.

## مهم
1. ارفع محتويات المشروع إلى GitHub كالمعتاد.
2. انشر التحديث على Render.
3. لا تستخدم REBUILD.
4. قاعدة البيانات الحية على /var/data/crm.db لا يتم استبدالها.
5. بعد نجاح النشر، استخدم من داخل CRM خيار استيراد إثراء مقاول وارفع الملف المنفصل:
   muqawil_enrichment_merged_15966_V7_5_12.json

## الترتيب
- الشركات التي لديها واتساب أو جوال موثوق تظهر أولاً.
- داخلها ترتيب التصنيف:
  غير مصنف → غير محدد → السادسة → الخامسة → الرابعة → الثالثة → الثانية → الأولى.
- الشركات التي ليس لديها واتساب/جوال تظهر في نهاية القائمة، وبنفس ترتيب التصنيف.
- عند إضافة جوال/واتساب يدوياً أو اعتماد جوال من Google Places، يعاد ترتيب الشركة تلقائياً.
- التسلسل الظاهر # يعاد حسابه تلقائياً حسب ترتيب النتائج.
- الهاتف الثابت من Google Places لا يعامل كرقم واتساب.
