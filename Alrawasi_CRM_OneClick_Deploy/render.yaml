services:
  - type: web
    name: alrawasi-crm
    runtime: docker
    plan: free
    rootDir: Alrawasi_CRM_OneClick_Deploy
    healthCheckPath: /health
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 5000
