# V7.5.31
1. حذف الشركة يعود إلى نفس صفحة الشركات مع نفس البحث والفلاتر وعدد العناصر في الصفحة.
2. رابط العودة محفوظ كـ return_to داخلي وآمن.
3. زر «نسخة JSON» لمدير النظام يصدر Snapshot منطقي كامل للقاعدة الحية.
4. ملف JSON يتضمن كل جداول CRM الحالية + counts + رقم إصدار CRM + وقت التصدير.
5. التصدير Read-Only ولا يغيّر قاعدة البيانات.
6. هذا إصدار كود فقط ولا يستبدل /var/data/crm.db.
