# V7.5.25
إصلاح Internal Server Error في صفحة استعادة البيانات.
السبب: صفحة Preview كانت تنفذ 13,288 استعلام SQLite منفصل عند كل فتح.
الإصلاح: تحميل مفاتيح الشركات والجوالات الحية باستعلام واحد ثم المقارنة في الذاكرة.
لا تغيير على قاعدة البيانات عند فتح الصفحة.
لا حذف ولا استبدال للجوالات.
