# V7 - بيانات دائمة
قاعدة التشغيل محفوظة على Render Persistent Disk في `/var/data/crm.db`.
يتم نسخ قاعدة المشروع إلى القرص في أول تشغيل فقط، وبعدها لا يستبدل النشر البيانات.
مزامنة مقاول إضافة فقط: تضيف المقاول الجديد ولا تعدل أي عميل موجود أو حالته أو مندوبه أو سجل التواصل.
تم تحويل خدمة الويب في Blueprint إلى Starter لأن Persistent Disk لا يعمل على Free.
